<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.gestures"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:label="GesturesLauncher"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".GestureCaptureActivity" android:exported="false"/>
        <activity android:name=".MappingActivity" android:exported="false"/>

        <service
            android:name=".GestureOverlayService"
            android:exported="false"
            android:foregroundServiceType="location|mediaPlayback|connectedDevice"/>
    </application>
</manifest>
